//Utilizaremos o .concat para concatenar dua listas de chamadas

const turma1 = ['Amanda', 'Benicio', 'Theo'];
const turma2 = ['Isabela', 'Arthur', 'Maitê'];

const turmaUnificada = turma1.concat(turma2);

console.log(`A lista das duas turmas juntas é: ${turmaUnificada}`);